{% extends "base.html.twig" %}

{% block body %}
    <div class="row justify-content-center">
        <div class="card text-center">
            <div class="card-header">{% trans %}login.header{%endtrans%}
            </div>
            <div class="card-body">
                {{ include('@FOSUser/Security/login_content.html.twig') }}
            </div>
        </div></div>
    {% endblock body %}
